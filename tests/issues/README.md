# Issue regressions

Add a focused subdirectory here when a reported compiler issue needs permanent
regression coverage. Name it after the issue identifier and register its tests
from `tests/CMakeLists.txt` or the applicable version directory.
