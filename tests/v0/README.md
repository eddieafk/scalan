# Development archive

This directory preserves examples, fixtures, and smoke tests from the compiler's
development before the versioned test model was introduced. It is kept as a
reference history and is not part of the active test build.

New coverage belongs in the appropriate version directory under `tests/`, using
its `val`, `inval`, `run`, or named `smoke` test structure.
