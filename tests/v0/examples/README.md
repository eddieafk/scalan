# Historical examples

These programs record compiler features and regression scenarios from the
project's development before versioned tests were introduced. They are retained
as reference material and are not part of the active test suite.

New executable coverage belongs in the `run` directory of the appropriate
version under `tests/`.
